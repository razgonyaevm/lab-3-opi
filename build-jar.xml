<?xml version="1.0" encoding="UTF-8"?>
<project name="build-jar" basedir=".">

    <property file="build.properties"/>

    <tstamp>
        <format property="TODAY" pattern="yyyy-MM-dd HH:mm:ss"/>
    </tstamp>

    <target name="compile">
        <mkdir dir="${param.classes}"/>
        <javac srcdir="${param.src}"
               destdir="${param.classes}"
               source="${compiler.source}"
               target="${compiler.target}"
               includeantruntime="false"
               debug="true">
            <classpath>
                <fileset dir="${lib.dir}" includes="**/*.jar"/>
            </classpath>
            <compilerarg value="-processorpath"/>
            <compilerarg value="${lib.dir}/lombok-1.18.40.jar"/>
        </javac>
        <copy todir="${param.classes}">
            <fileset dir="${resources.dir}" excludes="**/*.java"/>
        </copy>
        <echo message="[COMPILE] Компиляция завершена"/>
    </target>

    <target name="build" depends="compile">
        <manifest file="${build.dir}/MANIFEST.MF">
            <attribute name="Main-Class" value="${param.main}"/>
            <attribute name="Implementation-Version" value="${project.version}"/>
            <attribute name="Built-By" value="${user.name}"/>
            <attribute name="Build-Date" value="${TODAY}"/>
        </manifest>
        <jar destfile="${dist.dir}/${param.name}.jar"
             basedir="${param.classes}"
             manifest="${build.dir}/MANIFEST.MF"/>
        <echo message="JAR создан: ${dist.dir}/${param.name}.jar"/>
    </target>

</project>